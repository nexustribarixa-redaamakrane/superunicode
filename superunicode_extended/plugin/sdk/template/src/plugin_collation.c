/**
 * <PLUGIN-ID> — Optional Per-Plugin Collation (ExtUCA)
 *
 * Defines ordering constraints for this plugin's codepoints. Leave empty to
 * inherit the default ExtUCA ordering.
 */

#include "superunicode_extended/plugin.h"

/* Example: none — default ordering applies. */
