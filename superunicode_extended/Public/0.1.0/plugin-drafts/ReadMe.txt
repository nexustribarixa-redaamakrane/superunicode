# /Public/0.2.0 plugin drafts
# Draft plugin packages under consideration. NOT normative. To get a
# plugin approved: complete the SDK template, validate with plugin_verify,
# and land it in extsucd/plugins/ + registry.txt at the next release.
