/**
 * <PLUGIN-ID> — Optional Mapping Tables
 *
 * ExtSUCS maps from a mounted plugin's codepoints back to Base SUCS where a
 * 1:1 meaning exists (sucs_downcast semantics). Fill in only what applies.
 */

#include "superunicode_extended/plugin.h"

/* Example: none — this plugin has no Base SUCS counterparts. */
